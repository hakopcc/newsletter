						            <div class="col-md-8">
						                <div class="panel panel-form">
						                    <div class="form-group">
                                                <div class="panel-heading">{{ "Associate an event to this listing"|trans({},'messages',lang) }}</div>
						                        <div class="panel-body">
                                                    <div class="col-sm-12">
							                            <div class="form-group row">
							                                <div class="col-sm-12">
							                                    <a href="/{{ getParameter('alias_sitemgr_module') }}/content/event/event.php?listing_attached={{ listingId }}&account_id={{ accountId }}" class="btn btn-primary">
							                                        {{ "Add a new Event"|trans({},'messages',lang) }}
							                                    </a>
							                                    <br>
							                                </div>
							                            </div>
							                            <div class="form-group row">
	                                                        <div class="col-sm-10">
							                                    <label>
							                                        {{ "or"|trans }}
							                                        {{ "Associate an existing Event with this Listing"|trans({},'messages',lang) }}
						                                    	</label>
                                                                <input type="hidden" name="listing_id" value="{{ listingId }}">
                                                                <input type="hidden" name="account_id" value="{{ accountId }}">
{% if allEventsAvailable and error is empty %}
                                                                <div id="eventSelectBox"></div>
                                                                <p class="help-block small">{{ "Leave it blank to unlink the Event."|trans({},'messages',lang) }}</p>
{% elseif error is empty %}
                                                                <p class="alert alert-info">{{ 'No event available for association with this Listing. In order to be linked to this Listing, the event should not be associated with other Listing and should belong to the same account.'|trans({},'messages',lang) }}</p>
{% else %}
                                                                <p class="alert alert-danger">{{ error }}</p>
{% endif %}
						                                </div>
						                            </div>
						                            </div>

						                        </div>

						                    </div>
						                </div>
						            </div>
                                    <div class="col-sm-4">
                                        <br>
                                        <div class="panel panel-default">
                                            <div class="panel-body">
										        <p>{{ "The event is linked from the listing."|trans({},'messages',lang) }}</p>
										    </div>
										</div>
									</div>
