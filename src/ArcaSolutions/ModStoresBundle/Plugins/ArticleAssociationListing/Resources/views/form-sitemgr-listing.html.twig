                                    <div class="col-md-8">
                                        <div class="panel panel-form">
                                            <div class="form-group">
                                                <div class="panel-heading">{{ "Associate an article to this listing"|trans({},'messages',lang) }}</div>
                                                <div class="panel-body">
                                                    <div class="col-sm-12">
                                                        <div class="form-group row">
                                                            <div class="col-sm-12">
                                                                <a href="/{{ getParameter('alias_sitemgr_module') }}/content/article/article.php?listing_attached={{ listingId }}&account_id={{ accountId }}" class="btn btn-primary">
                                                                    {{ "Add a new Article"|trans({},'messages',lang) }}
                                                                </a>
                                                                <br>
                                                            </div>
                                                        </div>
                                                        <div class="form-group row">
                                                            <div class="col-sm-10">
                                                                <label>
                                                                    {{ "or"|trans({},'messages',lang) }}
                                                                    {{ "Associate an existing Article with this Listing"|trans({},'messages',lang) }}
                                                                </label>
                                                                <input type="hidden" name="listing_id" value="{{ listingId }}">
                                                                <input type="hidden" name="account_id" value="{{ accountId }}">
{% if allArticlesAvailable and error is empty %}
                                                                <div id="articleSelectBox"></div>
                                                                <p class="help-block small">{{ "Leave it blank to unlink the Article."|trans({},'messages',lang) }}</p>
{% elseif error is empty %}
                                                                <p class="alert alert-info">{{ 'No article available for association with this Listing. In order to be linked to this Listing, the article should not be associated with other Listing and should belong to the same account.'|trans({},'messages',lang) }}</p>
{% else %}
                                                                <p class="alert alert-danger">{{ error }}</p>
{% endif %}
                                                            </div>
                                                        </div>
                                                    </div>

                                                </div>

                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-sm-4">
                                        <br>
                                        <div class="panel panel-default">
                                            <div class="panel-body">
                                                <p>{{ "The article is linked from the listing."|trans({},'messages',lang) }}</p>
                                            </div>
                                        </div>
                                    </div>
